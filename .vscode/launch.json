{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run words (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8787/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "words: build + serve (debug)"
    }
  ]
}
